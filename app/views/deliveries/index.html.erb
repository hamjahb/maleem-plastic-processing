<h1>Deliveries#index</h1>
<p>Find me in app/views/deliveries/index.html.erb</p>

<p id="notice"><%= notice %></p>

<h1>Active Delivery Orders</h1>

<table class="table table-striped  table-hover table-bordered">
  <thead>
    <tr class="text-center">
      <th>Customer</th>
      <th>Specifications</th>
      <th>Order weight</th>
      <th>Total Boxes Complete</th>
      <th>Total Boxed Weight</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr class="text-center">
        <td><%= order.customer_name %></td>
        <td><%= order.specifications %></td>
        <td><%= order.order_weight_kg %> KG</td>
        <td><%= order.total_boxes %> Boxes</td>
        <td><%= weight_of_cut_boxes(order.total_boxes, order.packing_weight) %> KG</td>

        <td> 
          <%= link_to deliveries_view_path(order_id: order.id) do %>
            <button class="btn btn-outline-success">Deliver Order</button>
          <% end %>
        </td>

        <td> 
          <%= link_to order do %>
            <button class="btn btn-outline-info" >Order Details</button>
          <% end %>
        </td>
        
      </tr>
    <% end %>
  </tbody>
</table>
