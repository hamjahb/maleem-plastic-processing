<body class="text-center">
    <h1>Editing Roll <%= @roll.id %></h1>

    <%= render 'form', roll: @roll %>
     <button type="button" class="btn btn-success btn-lg">
     <%= link_to 'back', @roll %>
  </button>
</body>
