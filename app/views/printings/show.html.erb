<h1>Print Order for <%= @order.customer_name%></h1>
<h4><%= @order.specifications%><h4>



<p id="notice"><%= notice %></p>

<h3>Rolls Ready for printing</h3>

<table class="table table-striped  table-hover table-bordered">
  <thead>
    <tr>
      <th>Roll ID</th>
      <th>Roll weight</th>
      <th>Printed<th>

      <th colspan="1"></th>
    </tr>
  </thead>

  <tbody>
    <% @rolls.each do |roll| %>
      <tr>
        <td><%= roll.id %></td>
        <td><%= roll.roll_weight %> KG</td>
        <td><%= roll.printed_weight %> KG</td>

        <td> 
          <%= link_to printings_edit_path(id: roll.id) do %>
            <button class="btn btn-outline-success">Roll Print</button>
          <% end %>
        </td>
        
      </tr>
    <% end %>
  </tbody>
</table>
<br>

<div class="text-center">
  <%= link_to printings_path do %>
    <button class="btn btn-outline-success btn-lg ">Back</button>
  <% end %>
</div>

